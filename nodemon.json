{
	"verbose": true,
	"ignore": ["*.test.js", "fixtures/*", "views/*", "public/*", "dist/*"],
	"watch": ["src"],
	"ext": "ts",
	"exec": "rm -rf dist && node esbuild.js && node dist/server.js",
	"legacyWatch": true
}
